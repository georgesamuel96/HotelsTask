package com.example.georgesamuel.dubaihotels.materialComponent.adapter;

import android.content.Context;
import android.widget.ArrayAdapter;

import com.example.georgesamuel.dubaihotels.materialComponent.model.User;

public class AlertDialogAdapter extends ArrayAdapter<User> {

    public AlertDialogAdapter(Context context, int resource) {
        super(context, resource);
    }
}
